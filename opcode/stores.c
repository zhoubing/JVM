//
// Created by zhoubing on 2022/11/17.
//

#include <assert.h>
#include "../runtime/frame.h"

void Read_istore(Frame *frame) {
    assert(0);
}

int Run_istore(Frame *frame) {
    assert(0);
}

void Read_lstore(Frame *frame) {
    assert(0);
}

int Run_lstore(Frame *frame) {
    assert(0);
}

void Read_fstore(Frame *frame) {
    assert(0);
}

int Run_fstore(Frame *frame) {
    assert(0);
}

void Read_dstore(Frame *frame) {
    assert(0);
}

int Run_dstore(Frame *frame) {
    assert(0);
}

void Read_astore(Frame *frame) {
    assert(0);
}

int Run_astore(Frame *frame) {
    assert(0);
}

void Read_istore_0(Frame *frame) {
    assert(0);
}

int Run_istore_0(Frame *frame) {
    assert(0);
}

void Read_istore_1(Frame *frame) {
    assert(0);
}

int Run_istore_1(Frame *frame) {
    assert(0);
}

void Read_istore_2(Frame *frame) {
    assert(0);
}

int Run_istore_2(Frame *frame) {
    assert(0);
}

void Read_istore_3(Frame *frame) {
    assert(0);
}

int Run_istore_3(Frame *frame) {
    assert(0);
}

void Read_lstore_0(Frame *frame) {
    assert(0);
}

int Run_lstore_0(Frame *frame) {
    assert(0);
}

void Read_lstore_1(Frame *frame) {
    assert(0);
}

int Run_lstore_1(Frame *frame) {
    assert(0);
}

void Read_lstore_2(Frame *frame) {
    assert(0);
}

int Run_lstore_2(Frame *frame) {
    assert(0);
}

void Read_lstore_3(Frame *frame) {
    assert(0);
}

int Run_lstore_3(Frame *frame) {
    assert(0);
}

void Read_fstore_0(Frame *frame) {
    assert(0);
}

int Run_fstore_0(Frame *frame) {
    assert(0);
}

void Read_fstore_1(Frame *frame) {
    assert(0);
}

int Run_fstore_1(Frame *frame) {
    assert(0);
}

void Read_fstore_2(Frame *frame) {
    assert(0);
}

int Run_fstore_2(Frame *frame) {
    assert(0);
}

void Read_fstore_3(Frame *frame) {
    assert(0);
}

int Run_fstore_3(Frame *frame) {
    assert(0);
}

void Read_dstore_0(Frame *frame) {
    assert(0);
}

int Run_dstore_0(Frame *frame) {
    assert(0);
}

void Read_dstore_1(Frame *frame) {
    assert(0);
}

int Run_dstore_1(Frame *frame) {
    assert(0);
}

void Read_dstore_2(Frame *frame) {
    assert(0);
}

int Run_dstore_2(Frame *frame) {
    assert(0);
}

void Read_dstore_3(Frame *frame) {
    assert(0);
}

int Run_dstore_3(Frame *frame) {
    assert(0);
}

void Read_astore_0(Frame *frame) {
    assert(0);
}

int Run_astore_0(Frame *frame) {
    assert(0);
}

void Read_astore_1(Frame *frame) {
    assert(0);
}

int Run_astore_1(Frame *frame) {
    assert(0);
}

void Read_astore_2(Frame *frame) {
    assert(0);
}

int Run_astore_2(Frame *frame) {
    assert(0);
}

void Read_astore_3(Frame *frame) {
    assert(0);
}

int Run_astore_3(Frame *frame) {
    assert(0);
}

void Read_iastore(Frame *frame) {
    assert(0);
}

int Run_iastore(Frame *frame) {
    assert(0);
}

void Read_lastore(Frame *frame) {
    assert(0);
}

int Run_lastore(Frame *frame) {
    assert(0);
}

void Read_fastore(Frame *frame) {
    assert(0);
}

int Run_fastore(Frame *frame) {
    assert(0);
}

void Read_dastore(Frame *frame) {
    assert(0);
}

int Run_dastore(Frame *frame) {
    assert(0);
}

void Read_aastore(Frame *frame) {
    assert(0);
}

int Run_aastore(Frame *frame) {
    assert(0);
}

void Read_bastore(Frame *frame) {
    assert(0);
}

int Run_bastore(Frame *frame) {
    assert(0);
}

void Read_castore(Frame *frame) {
    assert(0);
}

int Run_castore(Frame *frame) {
    assert(0);
}

void Read_sastore(Frame *frame) {
    assert(0);
}

int Run_sastore(Frame *frame) {
    assert(0);
}
