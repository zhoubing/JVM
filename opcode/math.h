//
// Created by zhoubing on 2022/11/17.
//

#ifndef UNTITLED2_MATH_H
#define UNTITLED2_MATH_H

#include "../runtime/frame.h"

void Read_iadd(Frame *frame);

int Run_iadd(Frame *frame);

void Read_ladd(Frame *frame);

int Run_ladd(Frame *frame);

void Read_fadd(Frame *frame);

int Run_fadd(Frame *frame);

void Read_dadd(Frame *frame);

int Run_dadd(Frame *frame);

void Read_isub(Frame *frame);

int Run_isub(Frame *frame);

void Read_lsub(Frame *frame);

int Run_lsub(Frame *frame);

void Read_fsub(Frame *frame);

int Run_fsub(Frame *frame);

void Read_dsub(Frame *frame);

int Run_dsub(Frame *frame);

void Read_imul(Frame *frame);

int Run_imul(Frame *frame);

void Read_lmul(Frame *frame);

int Run_lmul(Frame *frame);

void Read_fmul(Frame *frame);

int Run_fmul(Frame *frame);

void Read_dmul(Frame *frame);

int Run_dmul(Frame *frame);

void Read_idiv(Frame *frame);

int Run_idiv(Frame *frame);

void Read_ldiv(Frame *frame);

int Run_ldiv(Frame *frame);

void Read_fdiv(Frame *frame);

int Run_fdiv(Frame *frame);

void Read_ddiv(Frame *frame);

int Run_ddiv(Frame *frame);

void Read_irem(Frame *frame);

int Run_irem(Frame *frame);

void Read_lrem(Frame *frame);

int Run_lrem(Frame *frame);

void Read_frem(Frame *frame);

int Run_frem(Frame *frame);

void Read_drem(Frame *frame);

int Run_drem(Frame *frame);

void Read_ineg(Frame *frame);

int Run_ineg(Frame *frame);

void Read_lneg(Frame *frame);

int Run_lneg(Frame *frame);

void Read_fneg(Frame *frame);

int Run_fneg(Frame *frame);

void Read_dneg(Frame *frame);

int Run_dneg(Frame *frame);

void Read_ishl(Frame *frame);

int Run_ishl(Frame *frame);

void Read_lshl(Frame *frame);

int Run_lshl(Frame *frame);

void Read_ishr(Frame *frame);

int Run_ishr(Frame *frame);

void Read_lshr(Frame *frame);

int Run_lshr(Frame *frame);

void Read_iushr(Frame *frame);

int Run_iushr(Frame *frame);

void Read_lushr(Frame *frame);

int Run_lushr(Frame *frame);

void Read_iand(Frame *frame);

int Run_iand(Frame *frame);

void Read_land(Frame *frame);

int Run_land(Frame *frame);

void Read_ior(Frame *frame);

int Run_ior(Frame *frame);

void Read_lor(Frame *frame);

int Run_lor(Frame *frame);

void Read_ixor(Frame *frame);

int Run_ixor(Frame *frame);

void Read_lxor(Frame *frame);

int Run_lxor(Frame *frame);

void Read_iinc(Frame *frame);

int Run_iinc(Frame *frame);

#endif //UNTITLED2_MATH_H
