//
// Created by zhoubing on 2022/11/18.
//

#ifndef UNTITLED2_CONVERSIONS_H
#define UNTITLED2_CONVERSIONS_H

#include "../runtime/frame.h"

void Read_i2l(Frame *frame);

int Run_i2l(Frame *frame);

void Read_i2f(Frame *frame);

int Run_i2f(Frame *frame);

void Read_i2d(Frame *frame);

int Run_i2d(Frame *frame);

void Read_l2i(Frame *frame);

int Run_l2i(Frame *frame);

void Read_l2f(Frame *frame);

int Run_l2f(Frame *frame);

void Read_l2d(Frame *frame);

int Run_l2d(Frame *frame);

void Read_f2i(Frame *frame);

int Run_f2i(Frame *frame);

void Read_f2l(Frame *frame);

int Run_f2l(Frame *frame);

void Read_f2d(Frame *frame);

int Run_f2d(Frame *frame);

void Read_d2i(Frame *frame);

int Run_d2i(Frame *frame);

void Read_d2l(Frame *frame);

int Run_d2l(Frame *frame);

void Read_d2f(Frame *frame);

int Run_d2f(Frame *frame);

void Read_i2b(Frame *frame);

int Run_i2b(Frame *frame);

void Read_i2c(Frame *frame);

int Run_i2c(Frame *frame);

void Read_i2s(Frame *frame);

int Run_i2s(Frame *frame);

#endif //UNTITLED2_CONVERSIONS_H
